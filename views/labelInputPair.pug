mixin labelInputPair(label, inputType, inputName, placeholder, required= true)
    div.inlined
        p= label
        input(type= inputType, name= inputName, placeholder= placeholder, required="#{required}")